# =============================================================
#  MonitoringAlert — configuration
#
#  Editez ce fichier AVANT de lancer install.bat
#  Ce fichier sera copié dans %LOCALAPPDATA%\Programs\MonitoringAlert\
# =============================================================

# Dossier où sera installé l'exécutable monitoring-alert.exe
# Exemple : "D:\\Apps\\MonitoringAlert"
install_dir = "C:\\Program Files\\MonitoringAlert"

# Chemin complet de la base de données de températures
# Exemple : "D:\\Backup\\MonitoringAlert\\temperatures.db"
db_path = "C:\\Users\\Jordan\\AppData\\Local\\Programs\\MonitoringAlert\\temperatures.db"

# Dossier des fichiers de log (défaut : même répertoire que db_path)
# Les fichiers sont nommés monitoring-alert-YYYY-MM-DD.log (rotation quotidienne)
# Exemple : "D:\\Logs\\MonitoringAlert"
# log_dir = "C:\\Users\\Jordan\\AppData\\Local\\Programs\\MonitoringAlert"

# Intervalle de collecte du service (en secondes, minimum 60)
# Défaut : 300 (toutes les 5 minutes)
collect_interval_secs = 300

# Durée de rétention des données (en jours, minimum 360)
# Les snapshots plus anciens sont purgés automatiquement au démarrage
# du service puis toutes les 24h. La valeur minimale est 360 car l'algorithme
# compare une fenêtre de 180 jours (saisonnière) à la fenêtre précédente (180j).
retention_days = 365

# Adresse IP du serveur HTTP de LibreHardwareMonitor (Options → Remote Web Server)
# Défaut : "127.0.0.1" (machine locale)
lhm_host = "127.0.0.1"

# Port du serveur HTTP de LibreHardwareMonitor
# Défaut : 8085
lhm_port = 8085

# Niveau de log : "error" | "warn" | "info" (défaut) | "debug" | "trace"
# Passer à "debug" pour diagnostiquer un problème (collecte, capteurs, etc.)
log_level = "info"

# =============================================================
#  Rapports automatiques via notifications Windows
# =============================================================

# Rapport journalier
daily_report_enabled = true
daily_report_time    = "08:00"    # HH:MM (24h)

# Rapport hebdomadaire
weekly_report_enabled = false
weekly_report_day     = "MON"     # MON TUE WED THU FRI SAT SUN
weekly_report_time    = "08:00"

# Rapport mensuel
monthly_report_enabled = false
monthly_report_day     = 1        # 1-28
monthly_report_time    = "08:00"
